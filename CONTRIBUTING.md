# Contributing Guidelines

Thank you for contributing to **The ssrone** monorepo platform.

## Development Workflow
1. Use `pnpm install` at root to install workspace dependencies.
2. Run `pnpm run dev` to start all applications via Turborepo.
3. Follow the 5-part architecture for frontend modules and 5-layer pattern for backend microservices.
