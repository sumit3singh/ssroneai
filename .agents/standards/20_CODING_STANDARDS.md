# Coding Standards – BAITHAK ERP Charter #20

## TypeScript & React Rules
- Components MUST be under 300 lines of code.
- Explicit prop interface definitions for every React component.
- Strict type safety (`noImplicitAny`, zero `any` in business logic).
- Defensive coding: Always provide default fallback parameters (`categories = []`, `items = []`).
