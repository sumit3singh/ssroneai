# BAITHAK ERP – Testing Standard (TESTING_STANDARD.md)

## Automated Test Coverage
- Every module must maintain a `tests/` directory with unit and integration tests.
- Business services (`billing.service`, `discount.service`) must have 100% logic test coverage.
- UI build verification (`npm run build`) must compile with 0 errors.
