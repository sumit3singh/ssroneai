{
    "name": "@ssr-one-ai/charts",
    "version": "1.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc -p tsconfig.build.json --pretty",
        "test": "echo \"No tests yet for @ssr-one-ai/charts\" && exit 0"
    },
    "peerDependencies": {
        "react": "^18.0.0 || ^19.0.0",
        "recharts": "^2.0.0"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "@types/react": "^18.0.0 || ^19.0.0"
    }
}