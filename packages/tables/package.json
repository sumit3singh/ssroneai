{
    "name": "@ssr-one-ai/tables",
    "version": "1.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc -p tsconfig.build.json --pretty",
        "test": "echo \"No tests yet for @ssr-one-ai/tables\" && exit 0"
    },
    "peerDependencies": {
        "react": "^18.0.0"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "@types/react": "^18.0.0"
    }
}